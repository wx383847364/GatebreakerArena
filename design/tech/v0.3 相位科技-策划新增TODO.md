# Gatebreaker Arena v0.3 相位科技：策划新增 TODO

> **用途**：把最近两次提交中已进入工程的数据与资源，整理成后续功能开发可逐项勾选的策划工作清单。
>
> **对比版本**：`98ccf3c [33000060] 玩法：新增 v0.3 相位科技与界面内容` → `7b15441 [25000001] 配置：同步 v0.3 相位科技数据`。
>
> **状态口径**：`[x]` 已实现并具备代码/配置验证；`[~]` 已实现核心链路、仍需引擎内表现验收；`[ ]` 仍需策划或外部系统支持。

## 0.2 2026-08-31 P0–P2 闭环清单

### 玩法与确定性 `[x]`

- [x] 折光 P5 冷却只做递减，且最低 4 秒；覆盖初始 CD 低于、等于和高于 4 秒三档回归。
- [x] 同一固定帧的主球碰撞按真实 TOI 顺序派发，相位标记、穿透层与循环状态在每次碰撞后立即消费，不再用无序汇总结果反推。
- [x] `ItemLarge` 的阵营实时半径同时作用于主球、既有分裂球、新生成分裂球、逐帧碰撞和显示缩放；视觉尺寸与碰撞尺寸一致。
- [x] 主动技是否解锁、是否可用由玩法领域状态统一派生，UI 不再自行判断 P3 规则。

### LAN 与锁步 `[x]`

- [x] LAN 协议升级到不兼容的 v7；所有开局、输入、checksum、终局和返回大厅消息绑定 `SessionId + RoundId`。
- [x] 除房间广播外的业务包必须来自已绑定的可靠连接 ID；公开 endpoint 只用于发现与连接定位，不能认证 JoinResponse、快照、开局、结算或中止消息。
- [x] 断线和可靠发送失败已进入房间生命周期：大厅掉线由 Host 回填 AI，Loading/Playing 掉线统一中止，Client 丢失 Host 后进入明确错误状态。
- [x] Client 不接受入站 checksum authority；无效 schema、规则 hash 或配装 hash 的 StartLoading/Playing 快照会保持 Aborted，不会被后续状态推进覆盖。
- [x] 人类玩家每次进入 LAN Lobby 都必须显式确认五槽相位配装后才能 Ready；AI 仅镜像已确认的房主配装。
- [x] 输入提交、通用锁步运行时和 BrickDuel 共用单 Tick 最多 8 帧追帧预算；低帧率回归锁定该边界。
- [x] 对端返回大厅、回合变化、掉线或自动终止时统一失效旧局 UI generation，后台结算可继续，但不能把旧局状态写回新页面。

### UI、存档与异步安全 `[x]`

- [x] 相位配装、解锁确认、英雄 HUD、主动技按钮和结果正文采用显式 missing-only 运行时绑定；已有静态引用不会被覆盖，部分迁移不会重复创建整套面板。
- [x] 静态场景校验只负责已序列化的核心绑定，PlayMode smoke 负责验证 Awake 后生成的 v0.3 绑定，二者契约不再冲突。
- [x] 结果页可持续显示结算成功/失败重试状态；重复 HUD 刷新不会清空，离开结果页会清理。
- [x] 解锁、切英雄、切模式、LAN session/round 变化均由 operation generation 隔离；上下文失效会清除 pending，避免配装页永久锁死。
- [x] 相位结算第一次持久化失败会立即返回 UI 显示“正在重试”，同一 MatchId 的后台单飞任务继续重试，最终仍由档案服务幂等提交。
- [x] 文件存档在单实例内串行，使用 `.tmp` + 原子替换/可恢复 `.bak`；正式文件缺失时按 temp、backup、legacy、PlayerPrefs 顺序恢复。

### 本轮验证 `[x]/[~]`

- [x] `App.Shared`、`App.AOT`、完整 `App.HotUpdate`、Editor、EditMode Tests、PlayMode Tests 均通过同版本 Roslyn 编译。
- [x] 热更新边界扫描 0 failure / 0 warning；配置导出与校验工具 81 项测试通过；`git diff --check` 通过。
- [~] 当前机器仍缺有效团结编辑器 License，Unity Test Runner、双机真机长局、断网重连和多分辨率视觉验收需在可用编辑器/设备上执行。

## 0.1 2026-08-31 子代理审核修复记录

### 已修复 `[x]`

- LAN 客户端恢复房间快照时会复制并校验 `PhaseHeroId`、五槽 `PhaseTechIds` 和 `PhaseLoadoutHash`，不会再以空相位配装反复启动失败。
- BrickDuel 的 Loading ACK 已绑定到资源和运行时准备完成；UI 手动 ACK 不能绕过准备门槛，失败会终止本局，离开/重开使用操作代次隔离旧异步任务。
- LAN BrickDuel 使用 30Hz 累加器按确认帧推进，单次最多追 8 帧且不丢弃积压时间；终局必须由参与客户端对最终帧、checksum 和全局结果达成一致后才允许结算。
- 确定性碰撞尺寸只读取已进入规则哈希的配置，Prefab Renderer Bounds 仅用于调试显示，不再反向修改玩法规则。
- 蜃影 P4 可从主球确定性生成两颗临时球；裂痕/脉冲英雄穿透与道具覆盖层按共享池取最大值；折光镜界覆盖临时球，循环加速等待下一次挡板回球。
- 普通本地模式不再进入相位购买页；配装确认和科技解锁使用单飞及操作代次保护，双击、切英雄或切模式不会把异步结果应用到错误目标。
- 本地 BrickDuel 显式恢复下侧视角；已保存的英雄五槽配装会在初始化和切英雄时恢复。
- 相位结算返回结构化状态，持久化失败会回滚并保留重试资格；LAN 奖励只在终局共识后发放。
- LAN 协议升级至 v7；热更新和 EditMode 测试程序集通过同版本 Roslyn 编译，边界扫描 0 失败/0 警告，配置工具 81 项测试通过。

### 仍需引擎内验收 `[~]`

- 团结编辑器 Test Runner、双机长局、低帧率追帧、资源加载失败和结算失败后的 UI 提示仍需在有效 Editor License/真机环境执行。

## 0. 2026-08-30 落地执行记录

### 已落地 `[x]`

- 配置模型、加载器与导出器已支持结构化 `ResourceRule`、`RuntimeTuning`、主动技持续时间、科技离散步进、道具资源路径/边界和档案经济字段；加载时对 4 英雄、60 科技、9 道具、曲线、经济及 9 道具掉落表做完整约束校验。
- BrickDuel 固定帧运行时已接入双方相位配装、每秒 Φ 上限、分段升阶、四英雄资源、P3 主动技、P5 效果、主球碰撞遥测及 checksum；E 键为本地主动技输入。
- 裂穿共享池按“取当前与新授予的最大值”合并，并优先消耗道具覆盖层；分形不会递归复制临时球，蜃影主动技最多新增 2 球。
- 9 个正式道具已替换运行掉落表，400 份确定性袋精确表达 12.75% / 17% / 5% 权重；资源全部使用现有显式路径。
- C0–C5 相位砖潮曲线已成为相位对局运行权威；清砖 20 块会提前 1 秒预警，并向对手下一逻辑行按绿→红、红→黄升级一砖。
- 战前界面已改为 4 英雄 + P1–P5 五槽；默认科技免费，未解锁进阶科技按 15 币扣费解锁并持久化。新档案 15 币，正常结算胜/负/平分别 +12/+4/+4，MatchId 防重复，异常局不发币。
- HUD 已显示英雄、阶段、Φ、核心资源、共享穿透层和主动技 CD；旧英雄/芯片仅作为普通模式和本 LAN 兼容模拟的内部数据，不再作为相位配装页面内容。
- LAN 协议现为不向后兼容的 v7，房间固定 2 席，AI 自动补位并镜像房主已确认的相位配装；握手快照携带 `PhaseHeroId`、5 个 `PhaseTechIds`、配装哈希和本 Lobby 确认状态。联机确认帧现直接驱动双方 `BrickDuelRuntime`，技能输入、相位 checksum 与终局共识纳入同步；上侧客户端仅在显示层旋转视角，始终本机在下。

### 仍需引擎内/外部验收 `[~]`

- 团结编辑器当前机器缺少有效 Editor License，已通过同版本 Roslyn 响应文件完成 HotUpdate 与 EditMode 测试程序集编译，但尚未执行 Unity Test Runner 与真机视觉回归。
- 主动技、升阶、破阵预警目前复用运行时生成的文字 HUD 和主动技触控按钮；专属特效、音效及最终美术 Prefab 仍需资源支持。
- 排行榜正式后端、赛季规则、投降入口和账号级云存档不在本次本地可落地范围内，仍保留为外部依赖。

---

## 1. 本次提交实际新增了什么

### 1.1 已进入配置的数据资产 `[x]`

- 四名相位英雄：蜃影（数量）、脉冲（速度）、裂痕（穿透）、折光（弹道）。每名拥有 P1–P5 五级相位，共 20 条阶段效果。
- 相位科技：每英雄 5 槽 × 每槽 3 选项（1 基准 + 2 进阶）= 15 项；全量 60 项。基准免费，进阶单价 15 币；每张 `NetOffset` 固定 +30。
- 九种相位道具：裂穿、分形、缓滞、疾风、广域、磁吸、扩容、定向、核心缓冲；已配置基础掉落权重、资源与基础效果。
- 砖潮曲线：C0–C5，每 30 秒切换；破阵反馈为清 20 块砖、提前 1 秒预警。
- 全局经济常数：胜 +12 币、负 +4 币、科技解锁 15 币、Φ 每秒上限 6、目标操作差死亡时间差 >40 秒。
- 配置加载器、配置 Catalog 及加载测试已能读取 5 张 `DT_Phase*` 表；这只是“数据可读”，不是“玩法已生效”。

### 1.2 同批可独立验收的既有玩法/界面内容 `[x]`

- 原型期「定向反弹」已归一为正式九道具中的「定向」；旧 ID 只保留兼容别名，不再进入正式掉落袋。
- 现有砖潮球速从 3.0 调至 4.5；后期红/黄/神秘砖占比提高。
- 排行榜面板已接入场景，当前是本地 Mock 数据（名次、玩家、主英雄、ELO、胜率、场次）；后端数据源尚未接入。
- 四英雄头像、60 张科技图标、定向反弹图标/预制体已入库，可作为界面与玩法接入的资源基线。

### 1.3 已接入、待表现验收的关键链路 `[~]`

- [x] 相位 Φ 获取、逐段清零升阶、四英雄核心机制、P3 主动技与 P5 效果。
- [x] 科技解锁、存档、战前五槽配装、开局注入与道具效果修正。
- [x] 相位砖潮曲线及“清 20 块、预警 1 秒、升级对手下一行一砖”的破阵规则。
- [x] 本地 AI 和 LAN AI 镜像配装、局内文字 HUD、正常战后货币结算与异常局零奖励。
- [~] 移动端主动技按钮与交互链路已接通；专属技能特效/音效、升阶动画和破阵空间提示仍需资源与 Prefab 支持。

---

## 2. 先决策并回写配置（阻塞实现）

### P0：统一文本、时间与数值口径

- [x] **P3 冷却冻结为差异化数值。** 蜃影 12s、脉冲 18s、裂痕 20s、折光 16s；最低可减冷却为 4s。
- [x] **主动技持续时间结构化。** 蜃影 6s、脉冲 5s、折光镜面 6s；裂痕持续至共享穿透耗尽。
- [x] **四英雄 `ResourceRule` 与 `RuntimeTuning` 已结构化。** 客户端不再解析效果文案。
- [x] **主球口径已冻结。** 临时球不积累蜃影连击；穿透按实际归属球消费；破阵按实际摧毁砖统计；分形不递归分形。
- [x] **裂穿共享池已冻结。** 新授予与当前总层数取较大值、不相加；消耗时优先扣道具覆盖层，再扣英雄层。

### P0：确定版本边界

- [x] **v0.3 入口已替换为相位体系。** 旧英雄/芯片保留一个版本作为普通模式和兼容代码，但从相位配装及 LAN 协议入口不可达。
- [x] **正式掉落池冻结为九道具。** 裂穿、分形、缓滞、疾风、广域、磁吸、扩容、定向、核心缓冲；旧道具 ID 只做兼容映射。
- [ ] **确认排行榜的正式指标。** 当前 Mock 字段是 ELO、胜率、场次、主英雄。明确赛季、地区、同分排序、隐藏/封禁、离线显示和后端接口前，不应把 Mock 数值当成正式策划规则。

---

## 3. 英雄相位玩法待办

### 共通 Φ 成长 `[~]`

- [ ] 接道具 +2 Φ。
- [ ] 击碎神秘砖 +2 Φ。
- [ ] 击碎红砖 +0.5 Φ；黄砖 +1 Φ；绿砖 +0 Φ。
- [ ] 所有来源合计每秒最多 +6 Φ；明确同一帧多事件的截断与显示规则。
- [ ] 阶段阈值：P1=0、P2=30、P3=50、P4=70、P5=100。确认这是“累计总 Φ 阈值”（现有配置口径）而非“每级额外需求”；并在 HUD 文案中明确。
- [ ] 设计升阶反馈：特效、音效、暂停/不暂停、相位效果何时生效、满级后的 Φ 去向。
- [ ] 验收：相同随机种子下，Φ、相位、核心资源必须进入确定性状态与 checksum；科技仅在开局注入，不进入逐帧同步。

### 蜃影（数量 / 连击 / 分形） `[~]`

- [ ] P1：主球连击 8 次，分裂 1 个 6 秒临时球。
- [ ] P2：阈值改为连击 6 次。
- [ ] P3「幻潮」：复制场上所有球，最多新增 2 个临时球、持续 6 秒；按 P0 决议配置真实 CD。
- [ ] P4：每次分裂改为生成 2 个临时球。
- [ ] P5：连击 4 次即分裂；场上每多 1 个物理球，挡板 +4%，最高 +12%。明确临时球消失、接球、断连时的实时回退。
- [ ] 断连规则：主球碰底部墙壁后连击清零，但球不消失、无失球惩罚。需确认是否与所有模式、所有相位生效。

### 脉冲（速度 / 节拍 / 疾风） `[~]`

- [ ] P1：每次挡板“有效改向”+1 节拍；3 秒未有效改向 -1；每层球速 +3%，上限 +12%。补充“有效改向”的角度阈值，避免站桩刷层。
- [ ] P2：节拍上限至 5；确认 P1 初始上限数值。
- [ ] P3「爆点」：锁定当前最高节拍 5 秒、速度上限 6.0；确定与疾风道具/球速硬上限的叠加顺序。
- [ ] P4：每节拍 +5%，总上限 +20%。
- [ ] P5：节拍 >4 时，每第 3 次命中得 1 次穿透；明确计数是否跨球、是否受穿透命中影响、最多可存多少。

### 裂痕（穿透 / 蓄力 / 裂穿） `[~]`

- [ ] P1：主球命中砖块 +1 蓄力，5 点获得 1 次穿透，最多存 2 次。
- [ ] P2：穿透存储上限 3。
- [ ] P3「贯裂」：立即获得 4 次穿透并 +12% 速度，至穿透耗尽；需裁定与“最多存 3 次”冲突时是临时突破、覆盖还是上限调整。
- [ ] P4：满蓄力释放由 1 次改为 2 次穿透，上限仍 3。
- [ ] P5：4 点蓄力即触发；穿透命中返 1 点蓄力。明确返还是否可在同次穿透链中循环。
- [ ] 主球触底蓄力清零、球不消失；与蜃影断连一致，需做统一规则与 UI 提示。

### 折光（弹道 / 标记 / 广域） `[~]`

- [ ] P1：挡板回球获得折射标记；带标记命中砖块完成循环；下一次挡板回球 +8% 速度、持续 3 秒。
- [ ] P2：循环速度奖励 +12%，标记持续时间“增加到 3 秒”。此处与 P1 的 3 秒相同，需确认 P1 原始标记时长是否应小于 3 秒，或 P2 应为其他值。
- [ ] P3「镜界」：本方半场 6 秒镜面，只碰撞本方球。补充镜面位置、尺寸、反射规则、与定向反弹/穿透/多球的优先级。
- [ ] P4：每轮循环 +6% 挡板回球速度，最多 3 层，总 +18%；标记时长 4 秒。明确层数何时清除。
- [ ] P5：每完成 2 次循环，镜界 CD -3 秒；确定最小 CD 与不可无限减 CD 的限制。

---

## 4. 科技、道具与经济待办

### 科技配装 `[~]`

- [ ] 每英雄固定 5 个相位槽，每槽三选一；进入配装页时自动填充 5 个“基准”，允许清空后替换。
- [ ] 进阶科技 40 项，单项 15 币解锁；基准免费。定义重复解锁、余额不足、解锁确认、退款、赛季重置、账号切换规则。
- [ ] 明确科技的生效范围：战前永久装备、仅本局生效、是否可局中换、是否允许不同模式拥有独立预设。
- [ ] 把 60 项科技的 `Effects` 作为唯一数值权威；`DT_TeckData.csv`、技能名称/描述表、UI 文案必须由同一源生成，禁止手填分叉。
- [x] 支持连续百分比与离散步进两类效果：裂穿/磁吸使用 `MagnitudeStep`，其余使用 `MagnitudePercent`；加载器已读取 `MagnitudeStep`、`BaseValue`、`ModifiedValue`、`ResolvedText`，运行时按百分比或离散次数应用。
- [ ] `NetOffset=+30` 当前是每张科技固定的平衡标签；明确是否只展示，或要在校验/数值矩阵中实际计算，避免被误作“总预算上限”。

### 九相位道具 `[~]`

| 道具 | 基础效果 | 掉落权重 | 价值权重 | 待确认点 |
|---|---|---:|---:|---|
| 裂穿 | 主球 2 次穿透 | 12.75% | 3 | 已与裂痕共池，取最大值合并 |
| 分形 | 1 个临时球，6 秒 | 12.75% | 3 | 临时球不递归分裂 |
| 缓滞 | 本方砖潮 -20%，6 秒 | 12.75% | 3 | 只修改本方砖潮速度 |
| 疾风 | 球速 +20%，5 秒 | 17% | 2 | 脉冲主动期间最终速度封顶 6.0 |
| 广域 | 挡板 +20%，8 秒 | 17% | 2 | 与蜃影 P5 乘算后受运行边界钳制 |
| 磁吸 | 下一个道具自动落向挡板 | 12.75% | 1 | 按离散次数消费 |
| 扩容 | 球体尺寸增幅 | 5% | 1 | 受配置 Min/Max 钳制 |
| 定向 | 挡板定向回弹 | 5% | 1 | 使用主球碰撞遥测 |
| 核心缓冲 | 抵消一次核心伤害 | 5% | 1 | 触发后消费 |

- [ ] 定义科技对道具“增强/削弱”的算法：对基础值加算或乘算、同类多效果是否只会来自单槽、下限（例如磁吸不能 <0）与显示舍入。
- [ ] 确认是否启用 `DropOffsetCap=10`。现有 60 项科技数据主要是效果修正，掉率偏移尚未形成可执行字段；如暂不启用，应在 UI 移除“掉率变化”承诺。
- [~] 九道具已映射现有图标/预制体并可运行；仍需补齐最终颜色、正/负/中性提示与专属表现验收。

### 砖潮与破阵反馈 `[~]`

- [ ] 按 C0–C5 采用相位曲线：0/30/60/90/120/150 秒切换，神秘砖占比为 5%/7%/7%/7%/7%/7%。
- [ ] 清砖 20 块后，向对手“下一逻辑行”注入一块升级砖，提前 1 秒预警。明确升级链（绿→红、红→黄、黄/神秘如何处理）、同帧多次触发、满行、砖潮推进中的落点。
- [ ] 确认当前 `DT_BrickDuelRule` 中的曲线调整（球速 4.5、后期更高硬砖率）与 `DT_PhaseCurve` 的关系：替换、叠加或分别用于不同模式。
- [ ] 验收：镜像公平不被破坏；破阵是可预见压力而非直接伤害；C5 能压制低操作玩家但不造成无解。

### 经济与长期线 `[ ]`

- [ ] 战后按胜 +12 / 负 +4 发币；明确投降、断线、AI、练习模式、重复结算、反作弊的奖励规则。
- [ ] 明确 40 个进阶科技全部解锁约需 600 币（约 50 胜）的目标是否符合留存预期；如含首胜/任务/赛季产出，需写入统一经济表。
- [ ] 设计存档字段：货币余额、已解锁 TechId、各英雄预设、最后选中英雄、版本号、迁移/回滚策略。

---

## 5. 界面、文案与资源待办

- [ ] 英雄选择：4 张英雄卡，展示定位、核心资源、核心道具、P1–P5 摘要；采用“选中 → 确认 → 科技配装”两步流。
- [ ] 科技配装：5 槽 × 3 选项，显示免费/15 币、已解锁/锁定、成对增益/削弱、实时价值矩阵、默认填充、清空、确认出战。
- [ ] 局内 HUD：当前 Φ/下一级阈值、P1–P5 状态、各英雄核心资源、P3 主动技 CD、临时效果时长、穿透层数与预警。
- [ ] 明确危险道具与普通道具的视觉语义；此前设计要求负面/危险物使用红或紫色，并预留 0.5 秒落地预警。九相位道具当前尚未完成这套美术分类。
- [ ] 排行榜：以接口替换 `LocalMockLeaderboardDataSource`；确定榜单名称、字段顺序、无数据/加载失败/离线状态和本地玩家固定行规则。
- [ ] 资源验收：四英雄立绘、科技图标、道具图标在实际分辨率和深色背景下可辨；图标 ID 与 `TechId`/`ItemId` 一一对应，缺图必须有占位策略。

---

## 6. 程序联调顺序与策划验收清单

建议按下面顺序推进，避免“有完整配置但玩法仍不可测”的断层。

1. [x] 冻结第 2 节程序口径，并回写 `DT_PhaseHero` / `DT_PhaseTech` / `DT_PhaseItem` / `DT_PhaseCurve` / `DT_PhaseMeta`。
2. [x] 实现 Φ、相位、核心资源并纳入确定性 checksum。
3. [x] 将九相位道具接到掉落与结算，并接入科技增强/削弱及边界钳制。
4. [x] 完成五槽配装、货币、解锁、存档与未解锁拦截；本版档案 schema 直接升级，不兼容旧相位档案。
5. [x] 接入砖潮 C0–C5 与破阵规则；文字预警已实现。
6. [~] 四英雄 P3/P5、AI、HUD 和移动端主动技按钮已完成代码链路；专属资源表现待补。
7. [ ] 进行首轮引擎内平衡测试：同地图、同随机种子下对比低操作/高操作玩家，目标死亡时间差 >40 秒；同时统计九道具实际出现率、接取率、科技选择率和胜率。

### 每次提测最低通过条件

- [x] 配置可加载，4 英雄、60 科技、9 道具、1 曲线、1 元数据均可读。
- [ ] 每项文案与实际数值一致；特别检查 P3 CD、持续时间、蜃影临时球 6 秒、折光 P2 标记时长。
- [~] 相位、道具和破阵状态已进入 checksum；仍需双机长局验证双方持续一致。
- [x] 科技未解锁不可装备，默认科技可直接出战，效果按开局配装注入正确一方。
- [~] 破阵文字预警及逻辑行升级已实现；空间特效和不同分辨率验收待完成。
- [~] 文字 HUD 可辨认英雄相位、Φ、核心资源、穿透层和主动技；临时效果图标/倒计时待 UI 资源支持。

---

## 7. 配置与参考入口

- 英雄相位：`Assets/Config/DT_PhaseHero.json`
- 科技权威数据：`Assets/Config/DT_PhaseTech.json`
- 道具权威数据：`Assets/Config/DT_PhaseItem.json`
- 曲线与破阵反馈：`Assets/Config/DT_PhaseCurve.json`
- 经济常数：`Assets/Config/DT_PhaseMeta.json`
- 完整科技可读清单：`design/tech/Gatebreaker Arena v0.3 科技清单.md`
- 相位/科技数值校对：`design/tech/Gatebreaker Arena v0.3 相位与科技数值校对.md`
- 战前界面 UX：`design/ux/Gatebreaker Arena v0.3 界面UX规格 v0.1.md`
- 历史落地迁移计划：`doc/方案与数据/Gatebreaker Arena 1v1 双向砖潮 v0.3 落地迁移计划 v0.1.md`

> 维护约定：任何数值、文案或机制裁定先改权威 JSON，再重新导出 CSV/说明/原型；提交时在本 TODO 对应项后附提交号和测试记录。
